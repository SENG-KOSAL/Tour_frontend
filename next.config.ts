import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "wallpapercave.com",
      "w0.peakpx.com",
      "i.pinimg.com",
      "images.unsplash.com",
      "admin.expatica.com",
    ],
  },
};

export default nextConfig;
